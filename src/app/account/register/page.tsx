import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Account - Register'
};

const RegisterPage: NextPage<Props> = ({}) => {
	return <></>;
};

export default RegisterPage;
