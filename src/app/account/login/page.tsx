import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Account - Login'
};

const LoginPage: NextPage<Props> = ({}) => {
	return <></>;
};

export default LoginPage;
