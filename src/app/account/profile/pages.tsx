import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Account - Profile'
};

const ProfilePage: NextPage<Props> = ({}) => {
	return <></>;
};

export default ProfilePage;
