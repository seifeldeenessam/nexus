import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Checkout - Shipping'
};

const ShippingPage: NextPage<Props> = ({}) => {
	return <></>;
};

export default ShippingPage;
