import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Checkout - Payment'
};

const PaymentPage: NextPage<Props> = ({}) => {
	return <></>;
};

export default PaymentPage;
