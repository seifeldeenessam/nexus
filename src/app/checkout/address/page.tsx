import type { Metadata, NextPage } from 'next';

type Props = {};

export const metadata: Metadata = {
	title: 'Nexus | Checkout - Address'
};

const AddressPage: NextPage<Props> = ({}) => {
	return <></>;
};

export default AddressPage;
